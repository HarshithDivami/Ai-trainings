pydantic-ai>=0.0.10  # adjust if newer required
logfire>=0.41.0      # version with updated Logfire init signature used by pydantic_graph
pydantic-graph>=0.0.9  # ensure installed; pulled transitively but explicit helps
python-dotenv>=1.0.0
streamlit>=1.38.0    # UI frontend framework
rich>=13.7.0         # Optional console formatting reuse
fastapi>=0.115.0     # Fast HTML API backend
uvicorn[standard]>=0.30.0  # ASGI server
jinja2>=3.1.4        # HTML templating
